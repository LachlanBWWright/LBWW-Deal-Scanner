model CsMarket {
    url        String  @unique
    displayUrl String
    maxPrice   Float
    maxFloat   Float
    lastPrice  Float
    queryId    String? @unique
    query      Query?  @relation(fields: [queryId], references: [id], onDelete: Cascade)
}
